# SAM repository guidance

The canonical Skill is `packages/sam-aobpg/`. Read its `SKILL.md` before modifying SAM behavior. Preserve portable pack schemas, decision support, factual safeguards, and approval gates. Never commit real client material; use the ignored `workspace/` directory.
